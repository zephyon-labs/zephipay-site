import { apiResponse,routeError } from "@/lib/paymentIntents/routeResponse";import {validRequestId}from"@/lib/paymentRequests/requests";import{callPaymentRequestApi}from"@/lib/paymentRequests/serverClient";
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;if(!validRequestId(id))return routeError(400,"A valid payment request ID is required.");return apiResponse(await callPaymentRequestApi({method:"GET",path:`/api/payment-requests/${encodeURIComponent(id)}`,requestId:request.headers.get("x-request-id"),response:"read"}) as never)}
