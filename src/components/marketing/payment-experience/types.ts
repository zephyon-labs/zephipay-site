export type PaymentField = {
  label: string;
  value: string;
};

export type PaymentStep = {
  label: string;
  description: string;
};

export type ReceiptField = {
  label: string;
  value: string;
};
